@model fmis.Models.UserModels.CombineIndexFmisUser

@{
    ViewData["Title"] = "Users";

}

﻿@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<link rel="stylesheet" href="~/assets/plugin/daterangepicker/daterangepicker.css">
<script src="~/assets/plugin/daterangepicker/moment.min.js"></script>
<script src="~/assets/plugin/daterangepicker/daterangepicker.js"></script>


<link href="https://cdnjs.cloudflare.com/ajax/libs/select2/4.1.0/css/select2.min.css" rel="stylesheet" />
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/select2/4.1.0/js/select2.min.js"></script>

<style>
    .hr {
        border: 1px solid black;
    }

    #nav {
        font-size: 12pt;
    }

    .pbutton {
        margin-left: 3px;
    }
</style>


<!-- Button trigger modal -->
@*<button type="button" class="btn btn-primary" data-toggle="modal" data-target="">
        Launch demo modal
    </button>*@

<!-- Modal -->
<div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Select Accounting Users</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <form method="post" asp-action="SaveUsers" id="employeeSearchForm">
                <div class="modal-body">
                    <div class="input-group">
                        <label for="employeeDropdown">Select Employee:</label>
                        <input type="text" id="searchInput" class="form-control" placeholder="Search employees" oninput="filterDropdown()">
                        <select id="employeeDropdown" name="selectedEmployee" class="form-control">
                            @foreach (var user in Model.Users)
                            {

                                <option value="@user.Id" data-employee-name="@user.Fname, @user.Lname">@user.Fname, @user.Lname</option>
                            }
                        </select>
                        @if (ViewData.ModelState.ContainsKey("Username"))
            {
                <div class="alert alert-danger">
                   @Html.ValidationMessage("Username");
                </div>
            }
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                    <button type="submit" class="btn btn-primary">Save changes</button>
                </div>
            </form>

        </div>
    </div>
</div>

<script>

    function filterDropdown() {
        var input, filter, select, options, option, i;
        input = document.getElementById("searchInput");
        filter = input.value.toUpperCase();
        select = document.getElementById("employeeDropdown");
        options = select.getElementsByTagName("option");

        for (i = 0; i < options.length; i++) {
            option = options[i];
            if (option.text.toUpperCase().indexOf(filter) > -1) {
                option.style.display = "";
            } else {
                option.style.display = "none";
            }
        }
    }




</script>


<!--<form method="get">
    <div class="form-group">
        <input type="text" name="searchQuery" class="form-control" placeholder="Search by username or email">
    </div>
    <div class="form-group">
        <label for="employeeDropdown">Select Employee:</label>
        <select class="form-control" id="employeeDropdown" name="selectedEmployee">
            <option value="">All Employees</option>-->
<!-- Add an option for all employees -->




<h3 class="grey lighter smaller">
    Accounting Users
</h3>
<hr>

<div class="row">
    <div class="col-md-1">
        <div class="form-group form-inline" style="margin-bottom: 10px;">
            <a class="btn btn-success btn-sm" data-toggle="modal" data-target="#exampleModal">
                <i class="ace-icon fa fa-plus "></i>Create New Users
            </a>
        </div>
    </div>
</div>
<br />


<table class="table table-striped" style="width: 900px">
    <thead>
        <tr>
            <th>
                Username
            </th>
            <th>
                Email
            </th>
            <th>
                Action
            </th>
        </tr>
    </thead>
    <tbody>

        @foreach (var item in Model.ListUser)
        {
            <tr>
                <td>@item.Username</td>
                <td>
                    @if (item.Email is null)
                    {
                        @("-");
                    }
                    else
                    {
                        @item.Email
                    }
                </td>
                <td>

                    <a class="btn btn-xs btn-danger" href="/Users/DeleteUser/@item.Id" onclick=" if (confirm('Are you sure you want to delete this into the database?')) {
                          // Save it!
                         Lobibox.notify('error', {
                               title: 'Deleted',
                                msg: 'User Successfully Deleted!',
                        });
                        }
                            else
                        {
                          return false
                        };">
                        <i class="ace-icon fa fa-trash"></i>
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>



@*<input type="hidden" id="selectedEmployeeFname" name="Fname" value="@employee.Fname" />
    <input type="hidden" id="selectedEmployeeLname" name="Lname" value="@employee.Lname" />
    <input type="hidden" id="selectedEmployeeId" name="Id_number" value="@employee.Id" />
    <input type="hidden" id="selectedEmployeerole" name="email" value="@employee.Email" />
    <input type="hidden" id="selectedEmployeeUsername" name="username" value="@employee.Username" />
    <input type="hidden" id="selectedEmployeepassword" name="password" value="@employee.Password" />
    <input type="hidden" id="selectedEmployeerole" name="role" value="@employee.Role" />*@